# Roadmap

Un item = un template = un commit. Chaque template doit passer `pytest` et
`nuclei -validate` avant d'être commité.

**Règle de rédaction** : le matcher doit prouver que l'API *répond sans
authentification*, avec une signature propre au produit (clé JSON, en-tête,
identifiant de version). Jamais un simple 200, jamais un titre de page seul.
Si l'endpoint annoncé ci-dessous ne correspond pas à la version courante du
produit, vérifie dans la documentation officielle avant d'écrire — mieux vaut un
`SKIP` qu'un template faux.

## Runtimes LLM

- [x] `templates/exposure/vllm-unauthenticated-api.yaml` — vLLM sert une API compatible OpenAI sans clé par défaut ; `GET /v1/models` renvoie `{"object":"list","data":[...]}`. Sévérité high.
- [x] `templates/exposure/text-generation-inference-exposed.yaml` — HuggingFace TGI expose `GET /info` avec `model_id` et `max_concurrent_requests`. Sévérité high.
- [x] `templates/exposure/localai-unauthenticated-api.yaml` — LocalAI, API compatible OpenAI sans authentification par défaut. Sévérité high.
- [x] `templates/exposure/lmstudio-server-exposed.yaml` — serveur local LM Studio, endpoint compatible OpenAI. Sévérité medium.
- [x] `templates/exposure/sglang-server-exposed.yaml` — serveur SGLang, `GET /get_model_info`. Sévérité high.
- [x] `templates/exposure/xinference-exposed.yaml` — Xorbits Inference, API de gestion de modèles. Sévérité high.
- [x] `templates/exposure/ollama-model-pull-abuse.yaml` — au-delà de la lecture : prouver que `/api/pull` est atteignable (sans déclencher de téléchargement). Sévérité high.
- [x] `templates/exposure/llamacpp-server-exposed.yaml` — llama.cpp (`llama-server`) : `GET /props` renvoie `default_generation_settings`, `total_slots`, `model_path`, `chat_template` et `build_info` — le chemin du modèle sur disque et le build prouvent qu'il s'agit bien de llama-server et qu'aucune clé n'est exigée (`--api-key` est facultatif, aucune authentification sans lui). Sévérité high.
- [x] `templates/exposure/text-embeddings-inference-exposed.yaml` — HuggingFace Text Embeddings Inference (TEI, le pendant « embeddings » de TGI) : `GET /info` renvoie la structure `Info` complète. Vérifié dans `router/src/lib.rs` (champs exacts de `pub struct Info`) : `model_id`, `model_sha`, `model_dtype`, `served_model_name`, `model_type` (variantes sérialisées `embedding` / `classifier` / `reranker`), `max_concurrent_requests`, `max_input_length`, `max_batch_tokens`, `max_client_batch_size`, `auto_truncate`, `tokenization_workers`, `version`, `docker_label`. Le trio `tokenization_workers` + `auto_truncate` + `max_client_batch_size` est propre à TEI et le distingue du `GET /info` de TGI déjà couvert. Vérifié dans `router/src/http/server.rs` : la route est câblée par `.route("/info", get(get_model_info))` et la couche d'autorisation n'est montée que dans un `if let Some(api_key) = api_key`, donc aucune vérification n'existe tant que `--api-key` n'est pas passé. Sévérité medium.
- [x] `templates/exposure/infinity-embedding-server-exposed.yaml` — Infinity (`michaelfeil/infinity`, serveur d'inférence embeddings / reranking compatible OpenAI) : `GET /models` renvoie une liste de modèles enrichie d'un objet `stats` — `{"data":[{"id":...,"stats":{"queue_fraction":...,"queue_absolute":...,"results_pending":...,"batch_size":...},"capabilities":...,"backend":...,"embedding_dtype":...,"lengths_via_tokenize":...,"device":...}]}`. Vérifié dans `libs/infinity_emb/infinity_emb/infinity_server.py` : ces clés viennent du `response_model=OpenAIModelInfo` de la route `GET {url_prefix}/models`, et aucun autre serveur compatible OpenAI n'ajoute `queue_fraction` / `results_pending` à `/models` — c'est donc la signature à matcher, jamais le statut HTTP. `route_dependencies = []` par défaut ; la dépendance `validate_token` n'est ajoutée que dans un `if api_key`, donc l'API répond sans jeton tant qu'aucune clé n'est configurée. Sévérité medium.
- [x] `templates/exposure/tabby-health-exposed.yaml` — Tabby (`TabbyML/tabby`, assistant de code auto-hébergé qui sert lui-même ses modèles de complétion) : `GET /v1/health` renvoie l'inventaire complet de la machine d'inférence — `{"model":...,"chat_model":...,"chat_device":...,"device":...,"cuda_devices":[...],"models":{"completion":...,"chat":...,"embedding":...},"arch":...,"cpu_info":...,"cpu_count":...,"version":{"build_date":...,"build_timestamp":...,"git_sha":...,"git_describe":...},"webserver":...}`. Vérifié dans `crates/tabby/src/services/health.rs` (champs exacts de `HealthState`, de la structure `Version` et du sous-objet `models`) et `crates/tabby/src/serve.rs` : la route est enregistrée par `"/v1/health", routing::get(routes::health).with_state(health_state)` (variante `POST` également) et aucun middleware d'authentification n'est monté sur le routeur, qui sert par ailleurs `/v1/completions` et `/v1/chat/completions`. Le trio `cpu_info` + `cuda_devices` + `git_describe` est propre à Tabby : matcher dessus, jamais sur le statut HTTP. Le champ `webserver` indique en prime si la couche de comptes est active. Sévérité medium.
- [x] `templates/exposure/koboldcpp-server-exposed.yaml` — KoboldCpp (`LostRuins/koboldcpp`, serveur d'inférence GGUF mono-binaire très répandu en auto-hébergement, écoute par défaut sur `:5001`) : `GET /api/extra/version` renvoie `{"result":"KoboldCpp","version":"<version>","protected":false,"txt2img":<bool>,"vision":<bool>,"transcribe":<bool>,"multiplayer":<bool>,"websearch":<bool>,"tts":<bool>,"embeddings":<bool>}`. Vérifié dans la documentation d'API embarquée du produit (`embd_res/kcpp_docs.embd`, exemple de réponse de `GET /api/extra/version`) et dans `koboldcpp.py` : `get_capabilities()` renvoie littéralement `{"result":"KoboldCpp", "version":KcppVersion, "protected":has_password, ...}` avec `has_password = (password!="")` et un global initialisé à `password = "" #if empty, no auth key required` — le serveur n'exige donc aucun jeton tant que `--password` n'est pas passé. La valeur littérale `"result":"KoboldCpp"` est la signature à matcher, et `"protected":false` prouve dans la même réponse que l'instance est ouverte : matcher sur ce couple, jamais sur le statut HTTP. Confirmer avec `GET /api/v1/model`, qui renvoie `{"result":"koboldcpp/<nom du modèle>"}`, et noter que les drapeaux `txt2img` / `tts` / `transcribe` annoncent les autres moteurs joignables sur la même instance. Sévérité high.
- [x] `templates/exposure/text-generation-webui-internal-api-exposed.yaml` — text-generation-webui (oobabooga/text-generation-webui, interface + serveur d'inférence auto-hébergé très répandu, dépôt actif en 2026) : `GET /v1/internal/model/info` renvoie `{"model_name":...,"lora_names":[...],"loader":...}`. Vérifié dans `modules/api/models.py` (fonction `get_current_model_info`, champs exacts) et `modules/api/script.py` : la dépendance `verify_api_key` ne rejette la requête que `if expected_api_key` (c'est-à-dire `shared.args.api_key` non vide) — tant que le flag `--api-key` n'est pas passé en plus de `--api`, la vérification est un no-op et l'intégralité de l'API compatible OpenAI (`/v1/chat/completions`, `/v1/completions`) reste joignable. Le trio `model_name`/`lora_names`/`loader` sur ce chemin est propre au produit. Port par défaut 5000. Sévérité high.

## Interfaces & plateformes d'agents

- [x] `templates/exposure/comfyui-unauthenticated.yaml` — ComfyUI, `GET /system_stats` renvoie les informations GPU et la version. Sévérité high.
- [x] `templates/exposure/langserve-exposed-playground.yaml` — LangServe expose `/docs` et les routes `/invoke` sans authentification. Sévérité high.
- [x] `templates/exposure/flowise-unauthenticated-api.yaml` — Flowise, `GET /api/v1/chatflows` sans authentification. Sévérité critical (les identifiants sont lisibles).
- [x] `templates/exposure/langflow-unauthenticated.yaml` — Langflow, API sans authentification. Sévérité critical.
- [x] `templates/exposure/open-webui-signup-enabled.yaml` — Open WebUI avec inscription ouverte : n'importe qui crée un compte. Sévérité medium.
- [x] `templates/exposure/anythingllm-exposed.yaml` — AnythingLLM sans authentification. Sévérité high.
- [x] `templates/exposure/dify-exposed-console.yaml` — console Dify accessible. Sévérité high.
- [x] `templates/exposure/litellm-proxy-no-master-key.yaml` — proxy LiteLLM sans `master_key` : `/v1/models` répond sans clé. Sévérité high.
- [x] `templates/exposure/gradio-app-exposed.yaml` — application Gradio exposée, `GET /config` renvoie la définition de l'interface. Sévérité medium.
- [x] `templates/exposure/automatic1111-api-exposed.yaml` — AUTOMATIC1111 Stable Diffusion WebUI lancé avec `--api` : `GET /sdapi/v1/sd-models` renvoie un tableau JSON d'objets `{"title":...,"model_name":...,"hash":...,"sha256":...,"filename":...,"config":...}` — les chemins de checkpoints sur disque prouvent l'accès à l'API de génération, `--api-auth` valant `None` par défaut (aucune authentification). Sévérité high.
- [x] `templates/exposure/letta-server-unauthenticated.yaml` — Letta (ex-MemGPT), plateforme d'agents à mémoire persistante : `GET /v1/health/` renvoie `{"version":"<version letta>","status":"ok"}` et `GET /v1/agents/` renvoie la liste des `AgentState` (`id`, `name`, `agent_type`, `llm_config`, `memory`) — le middleware de mot de passe n'est monté que si `LETTA_SERVER_SECURE=true` ou `--secure`, donc rien ne protège l'instance par défaut. Sévérité high.
- [x] `templates/exposure/librechat-open-registration.yaml` — LibreChat : `GET /api/config` est lisible sans authentification — le code le documente lui-même (`api/server/routes/config.js`, commentaire de `buildPreLoginPayload` : « readable by anonymous callers of `GET /api/config` »). La réponse révèle les fournisseurs d'authentification actifs et si l'inscription est ouverte. Matcher sur une clé de configuration propre à LibreChat, et signaler l'inscription ouverte sur une instance exposée. Sévérité medium.
- [x] `templates/exposure/hayhooks-status-exposed.yaml` — Hayhooks (deepset, serveur qui déploie des pipelines Haystack en API REST) : `GET /status` renvoie `{"status":"Up!","pipelines":["<nom de pipeline>", ...]}`. Vérifié dans `src/hayhooks/server/routers/status.py` : `status_all()` renvoie littéralement `StatusResponse(status="Up!", pipelines=registry.get_names())`, et `src/hayhooks/server/app.py` n'ajoute que `RequestIdMiddleware` et `CORSMiddleware` — aucune dépendance d'authentification ni vérification de clé d'API sur les routers montés (`status`, `deploy`, `undeploy`, `openai`…). Matcher sur la chaîne `Up!` conjointe à la clé `pipelines`, jamais sur le statut HTTP ; l'énumération des pipelines déployés prouve l'accès et le router `deploy` reste joignable. Sévérité high.
- [x] `templates/exposure/nemo-guardrails-server-exposed.yaml` — NVIDIA NeMo Guardrails (serveur FastAPI qui applique les garde-fous devant un LLM) : `GET /v1/rails/configs` renvoie `[{"id":"<identifiant de configuration>"}, ...]`, c'est-à-dire l'énumération des configurations de garde-fous chargées sur un chemin propre au produit. Vérifié dans `nemoguardrails/server/api.py` : `get_rails_configs()` renvoie littéralement `[{"id": config_id} for config_id in config_ids]` (ou `[{"id": app.single_config_id}]` en mono-configuration), et aucune dépendance ni middleware d'authentification n'est monté sur l'app — le même serveur expose `POST /v1/chat/completions` (proxy vers le LLM configuré, donc consommation aux frais de l'opérateur) et `POST /v1/checks`. Confirmer avec `GET /v1/health`, qui renvoie `{"status":"pass"}` avec le type de média `application/health+json`. Matcher sur le chemin `/v1/rails/configs` + un tableau JSON d'objets `id`, jamais sur le statut seul. Sévérité high.
- [x] `templates/exposure/chainlit-project-settings-exposed.yaml` — Chainlit (framework d'interfaces de chat LLM / agents, servi par `chainlit run`) : `GET /project/settings` renvoie la configuration complète de l'application, avec les clés `ui`, `features`, `userEnv`, `maskUserEnv`, `dataPersistence`, `threadResumable`, `threadSharing`, `markdown`, `chatProfiles`, `starters`, `starterCategories`, `debugUrl`. Vérifié dans `backend/chainlit/server.py` (route `@router.get("/project/settings")`, handler `project_settings`, clés exactes de la `JSONResponse`) et `backend/chainlit/auth/__init__.py` : `get_current_user()` commence par `if not require_login(): return None`, et `require_login()` n'est vrai que si `CHAINLIT_CUSTOM_AUTH` est défini ou qu'un `password_auth_callback` est déclaré — sans rappel d'authentification, la dépendance `UserParam` laisse donc passer l'appelant anonyme. Le couple `dataPersistence` + `chatProfiles` sur ce chemin est propre à Chainlit (à ne pas confondre avec le `GET /config` de Gradio déjà couvert) ; `userEnv` énumère les variables d'environnement que l'application réclame à l'utilisateur, et l'accès prouve que le back-end de chat — donc le LLM configuré — est joignable. Sévérité medium.
- [x] `templates/exposure/ragflow-config-exposed.yaml` — RAGFlow (infiniflow/ragflow, moteur RAG open source) : `GET /system/config` renvoie `{"data":{"registerEnabled":<bool>,"disablePasswordLogin":<bool>}}`, confirmé par `GET /system/version` (`{"data":{"version":"<version>"}}`) — vérifié dans `api/apps/restful_apis/system_api.py`, où ces routes (ainsi que `/system/ping` et `/system/healthz`) ne portent aucun décorateur `@login_required`, contrairement à `/system/status` et aux routes de gestion des tokens, elles bien protégées. La paire de clés `registerEnabled`/`disablePasswordLogin` est propre à RAGFlow et révèle sans compte si l'inscription ouverte est activée sur l'instance. Sévérité medium.
- [x] `templates/exposure/langgraph-server-unauthenticated.yaml` — LangGraph Server (langchain-ai/langgraph, plateforme d'exécution d'agents en graphe auto-hébergée, ex. conteneur `langgraph-api` lancé par `langgraph up` / `langgraph dev`) : `POST /assistants/search` (corps `{}`) renvoie un tableau d'objets `Assistant` — champs `assistant_id`, `graph_id`, `config`, `context`, `created_at`, `metadata`, `version`, `name`, `description` — confirmé par `GET /ok` (health check du serveur). Vérifié via la documentation officielle (`docs.langchain.com/langgraph-platform/auth`, section « No default authentication » : « Complete flexibility to implement your security model », valable pour tout déploiement auto-hébergé hors LangGraph Cloud managé) et le schéma `AssistantBase` du SDK (`reference.langchain.com/python/langgraph-sdk/schema/AssistantBase`). Le trio `assistant_id`/`graph_id`/`version` sur ce chemin est propre au produit ; l'accès, combiné à `/threads` et `/runs` tout aussi ouverts par défaut, permet l'exécution du graphe d'agent exposé. Sévérité high.
- [x] `templates/exposure/invokeai-runtime-config-exposed.yaml` — InvokeAI (`invoke-ai/InvokeAI`, interface et back-end de génération d'images auto-hébergé, du même écosystème que ComfyUI et AUTOMATIC1111 déjà couverts) : `GET /api/v1/app/runtime_config` renvoie `{"set_fields":[...],"config":{...}}`, c'est-à-dire l'intégralité de la configuration du serveur (`models_dir`, `outputs_dir`, `host`, `port`, `precision`, `attention_type`, `patchmatch`, `ram`, `vram`, `log_level`…) accompagnée de la liste des champs surchargés par l'opérateur. Vérifié dans `invokeai/app/api/routers/app_info.py` (route `GET /v1/app/runtime_config`, modèle de réponse `InvokeAIAppConfigWithSetFields` : `set_fields: set[str]`, `config: InvokeAIAppConfig`), `invokeai/app/services/config/config_default.py` (`multiuser: bool = Field(default=False, description="Enable multiuser support. When disabled, the application runs in single-user mode using a default system account with administrator privileges.")`) et `invokeai/app/api/auth_dependencies.py` (`get_current_user_or_default` commence par `if not config.multiuser: return TokenData(user_id="system", email="system@system.invokeai", is_admin=True)`) : hors mode multi-utilisateur, toutes les routes — y compris celles marquées `AdminUserOrDefault` — servent un appelant anonyme avec les droits d'administrateur. Le couple `set_fields` + `config` sur ce chemin est propre à InvokeAI ; confirmer avec `GET /api/v1/app/version` (`{"version":"<version>"}`) et `GET /api/v2/models/` (`{"models":[...]}`, gestionnaire de modèles également accessible en écriture). Matcher sur `set_fields`, jamais sur le statut HTTP. Sévérité high.
- [x] `templates/exposure/autogen-studio-no-auth.yaml` — AutoGen Studio (`microsoft/autogen`, paquet `python/packages/autogen-studio`, interface web d'assemblage et d'exécution d'équipes d'agents lancée par `autogenstudio ui`) : `GET /api/auth/type` renvoie `{"type":"none","exclude_paths":["/","/api/health","/api/version","/api/auth/login-url","/api/auth/callback-handler","/api/auth/callback","/api/auth/type"]}` — la valeur `"type":"none"` prouve dans la réponse elle-même que l'instance n'exige aucune authentification, et la liste `exclude_paths` sert de second fingerprint. Vérifié dans `autogenstudio/web/auth/models.py` (`class AuthConfig(BaseModel)` : `type: Literal["none", "github", "msal", "firebase"] = "none"`, avec exactement cette liste `exclude_paths` en valeur par défaut), `autogenstudio/web/auth/authroutes.py` (route `GET /type` renvoyant `{"type": auth_manager.config.type, "exclude_paths": auth_manager.config.exclude_paths}`) et `autogenstudio/web/auth/providers.py` (`NoAuthProvider` : `validate_token` renvoie `True` sans vérification et attribue l'utilisateur par défaut `guestuser@gmail.com` / `Default User`). `autogenstudio/web/app.py` monte bien `app.add_middleware(AuthMiddleware, auth_manager=auth_manager)`, mais devant des routers — `/api/sessions`, `/api/runs`, `/api/teams`, `/api/gallery`, `/api/settings`, `/api/mcp`, `/api/ws` — qui restent tous joignables tant que le type vaut `none`, c'est-à-dire tant que l'opérateur n'a rien configuré. Confirmer le produit avec `GET /api/version`, dont le handler renvoie littéralement `{"status": True, "message": "Version retrieved successfully", "data": {"version": VERSION}}`. Matcher sur le couple chemin `/api/auth/type` + `"type":"none"`, jamais sur le statut HTTP. Sévérité high.

## Bases vectorielles

- [x] `templates/exposure/chromadb-open-instance.yaml` — ChromaDB, `GET /api/v1/heartbeat` puis énumération des collections. Sévérité high.
- [x] `templates/exposure/qdrant-no-api-key.yaml` — Qdrant sans clé d'API : `GET /collections` répond. Sévérité high.
- [x] `templates/exposure/weaviate-anonymous-access.yaml` — Weaviate : `GET /v1/meta` renvoie `{"hostname":...,"version":"1.x.x","modules":{...}}`. `AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED` vaut `true` par défaut. Confirmer avec `GET /v1/schema`, qui passe, lui, par le contrôle d'accès. Sévérité high.
- [x] `templates/exposure/milvus-exposed.yaml` — ⚠️ Milvus parle surtout **gRPC** (19530) ; seul 9091 sert de l'HTTP (`/healthz`), et l'API RESTful v2 est sous `/v2/vectordb/`. Vérifier qu'une détection HTTP porteuse de sens existe — **si non, rendre `SKIP` immédiatement** plutôt que d'insister. Sévérité high.
- [x] `templates/exposure/marqo-unauthenticated.yaml` — Marqo (`marqo-ai/marqo`, moteur de recherche vectorielle/tensorielle qui embarque son propre modèle d'embedding) : `GET /` renvoie `{"message":"Welcome to Marqo","version":"<version>"}` et `GET /indexes` renvoie `{"results":[{"indexName":"<nom d'index>"}]}`. Vérifié dans `components/marqo/src/marqo/tensor_search/api.py` : le handler racine renvoie littéralement `{"message": "Welcome to Marqo", "version": version.get_version()}`, celui des index `{'results': [{'indexName': index.name} for index in indexes]}`, et aucune dépendance de clé d'API ni middleware d'authentification n'est déclaré sur l'application FastAPI ni sur ses routes. La chaîne `Welcome to Marqo` est propre au produit ; l'énumération des index prouve l'accès en lecture au moteur, pas seulement qu'un port répond. Matcher sur `Welcome to Marqo` puis confirmer sur `indexName`, jamais sur le statut HTTP. Sévérité high.
- [x] `templates/exposure/vespa-config-server-exposed.yaml` — Vespa (`vespa-engine/vespa`, moteur de recherche vectorielle et hybride employé comme couche de serving RAG à grande échelle) : sur le config server (port 19071 par défaut), `GET /application/v2/tenant/default` renvoie littéralement `{"message":"Tenant 'default' exists."}`, et `GET /application/v2/tenant/` énumère les tenants (`["default"]`). Vérifié dans la documentation officielle `docs.vespa.ai/en/reference/application-v2-tenant.html` : ces chemins, ce port et ces réponses exactes y sont documentés, de même que `PUT /application/v2/tenant/default` (`{"message":"Tenant default created."}`) et `DELETE` — la même API non authentifiée accepte donc l'écriture sur la configuration du cluster. `docs.vespa.ai/en/security/securing-your-vespa-installation.html` le confirme du côté produit : « Vespa's internal protocols are not authenticated by default and are therefore not safe in the face of untrusted network actors », « By default, the container allows unauthenticated writes to, and reads from, the Vespa installation », d'où l'avertissement « Vespa hosts MUST NOT be directly exposed to the public internet or to untrusted networks ». La chaîne `Tenant 'default' exists.` est propre au produit : matcher dessus, jamais sur le statut HTTP. Confirmer avec `GET /state/v1/version`, qui renvoie `{"version":"8.x.y"}` et qui est servi par tout service Vespa, config server compris (`docs.vespa.ai/en/reference/state-v1.html`) ; côté conteneur de requêtes (8080), `GET /state/v1/health` renvoie `{"time":...,"status":{"code":"up"},"metrics":{"snapshot":{"from":...,"to":...},"values":[...]}}`. Sévérité critical.

## MLOps & suivi d'expériences

- [x] `templates/exposure/mlflow-tracking-server-unauth.yaml` — MLflow, `GET /api/2.0/mlflow/experiments/search` sans authentification. Sévérité critical (lecture d'artefacts arbitraires).
- [x] `templates/exposure/jupyter-no-token.yaml` — Jupyter sans jeton : `GET /api/kernels` répond, donc exécution de code. Sévérité critical.
- [x] `templates/exposure/kubeflow-pipelines-exposed.yaml` — Kubeflow Pipelines : `GET /apis/v1beta1/pipelines` (ou `/pipeline/apis/v1beta1/...` derrière Istio) renvoie la liste JSON des pipelines. Sévérité high.
- [x] `templates/exposure/clearml-server-exposed.yaml` — ClearML : l'API (8008) répond sur `POST /debug.ping` ; le webserver (8080) sert l'app. Chercher la signature de version dans la réponse. Sévérité high.
- [x] `templates/exposure/label-studio-signup-open.yaml` — Label Studio : `GET /user/signup` sert le formulaire quand l'inscription est ouverte ; `GET /version` expose les versions des composants. Sévérité medium.
- [x] `templates/exposure/bentoml-yatai-exposed.yaml` — BentoML : le serveur sert `GET /livez`, `/readyz` et `GET /docs.json` (schéma OpenAPI listant les endpoints d'inférence). Sévérité high.
- [x] `templates/exposure/triton-inference-server-exposed.yaml` — NVIDIA Triton, `GET /v2/health/ready` puis index des modèles. Sévérité high.
- [x] `templates/exposure/aim-tracking-server-exposed.yaml` — Aim (aimhubio) : l'API est montée sous `/api` et `GET /api/projects/` renvoie `{"name":...,"path":...,"description":...,"telemetry_enabled":...,"warn_index":...,"warn_runs":...}` — le trio `telemetry_enabled` / `warn_index` / `warn_runs` est propre à Aim et le chemin du dépôt `.aim` sur le serveur fuite avec ; aucun routeur de l'app API n'a de dépendance d'authentification. Sévérité medium.
- [x] `templates/exposure/prefect-server-admin-exposed.yaml` — Prefect (serveur auto-hébergé) : `GET /api/admin/settings` renvoie l'intégralité de la configuration du serveur et `GET /api/admin/version` sa version. Vérifié dans `src/prefect/server/api/admin.py` : le `PrefectRouter(prefix="/admin")` ne déclare aucune dépendance d'authentification, et le serveur OSS n'en impose pas. Les secrets sont obfusqués mais le reste de la configuration ne l'est pas. Matcher sur `/api/admin/settings` + une clé de réglage propre à Prefect. Sévérité high.
- [x] `templates/exposure/torchserve-management-api-open.yaml` — TorchServe : l'API de *management* écoute par défaut sur le port 8081 et `GET /models` renvoie `{"nextPageToken":...,"models":[{"modelName":...,"modelUrl":...}]}`. Attention — la documentation officielle indique que l'autorisation par token est désormais **imposée par défaut** : le template ne détecte donc pas un défaut du produit mais une instance dont le token a été désactivé, ce qui autorise l'enregistrement d'un modèle arbitraire (classe ShellTorch). Matcher sur `nextPageToken` + `modelUrl`, jamais sur le statut seul. Sévérité critical.
- [x] `templates/exposure/feast-vector-stores-exposed.yaml` — Feast (feature store) : `GET /v1/vector_stores` énumère les magasins vectoriels. Vérifié dans `sdk/python/feast/feature_server.py` : la route n'est gardée que par `Depends(inject_user_details)`, sans effet lorsque Feast tourne dans sa configuration d'authentification par défaut. Chemin propre au produit, à préférer à `/health` qui n'est pas discriminant. Sévérité high.
- [x] `templates/exposure/dagster-webserver-exposed.yaml` — Dagster (webserver OSS, orchestrateur de pipelines ML) : `GET /server_info` (alias historique `/dagit_info`) renvoie `{"dagster_webserver_version":...,"dagster_version":...,"dagster_graphql_version":...}`. Vérifié dans `python_modules/dagster-webserver/dagster_webserver/webserver.py` : `webserver_info_endpoint` renvoie cette `JSONResponse` sans aucune dépendance d'authentification, et la documentation officielle (« Running Dagster locally ») liste explicitement « Authentication or web security features (`dg dev` does not include these) » comme absentes du produit OSS. Le trio de versions est propre à Dagster et confirme que l'endpoint `/graphql` — qui permet de lancer des runs — est atteignable. Matcher sur `dagster_webserver_version` + `dagster_graphql_version`. Sévérité high.
- [x] `templates/exposure/kedro-viz-exposed.yaml` — Kedro-Viz (visualisation de pipelines Kedro, lancé avec `--host 0.0.0.0`) : `GET /api/metadata` renvoie `{"has_missing_dependencies":<bool>,"package_compatibilities":[{"package_name":...,"package_version":...,"is_compatible":...}]}`. Vérifié dans `package/kedro_viz/api/rest/responses/metadata.py` (champs exacts du modèle `MetadataAPIResponse`) et `package/kedro_viz/api/rest/router.py` : le `APIRouter(prefix="/api")` ne déclare aucune dépendance d'authentification, et `package/kedro_viz/server.py` lance uvicorn sans middleware d'authentification. Le couple `has_missing_dependencies` / `package_compatibilities` est propre à Kedro-Viz ; confirmer avec `GET /api/main`, qui expose le graphe complet du pipeline. Sévérité medium.
- [x] `templates/exposure/mlserver-metadata-exposed.yaml` — MLServer (Seldon, le serveur d'inférence du protocole V2/KServe employé par Seldon Core et par `mlflow models serve`) : `GET /v2` renvoie `{"name":"mlserver","version":"<version>","extensions":[...]}`. Vérifié dans `mlserver/rest/app.py` (route `APIRoute("/v2", endpoints.metadata)` ; l'application ne monte que CORS, GZip, Prometheus et OpenTelemetry — aucune dépendance ni middleware d'authentification), `mlserver/handlers/dataplane.py` (`MetadataServerResponse(name=self._settings.server_name, version=self._settings.server_version, extensions=self._settings.extensions)`), `mlserver/types/dataplane.py` (champs exacts `name` / `version` / `extensions`) et `mlserver/settings.py` (`server_name` vaut `"mlserver"` par défaut, `host` `0.0.0.0`, `http_port` 8080). La valeur littérale `"name":"mlserver"` est la signature à matcher : elle distingue l'instance du `/v2` de Triton, déjà couvert. Les routes de gestion du dépôt de modèles (`/v2/repository/...`, chargement/déchargement) sont montées sur la même application sans authentification. Sévérité high.
- [x] `templates/exposure/metaflow-metadata-service-exposed.yaml` — Metaflow metadata service (`Netflix/metaflow-service`, le service qui stocke flows, runs, tasks et artefacts d'un déploiement Metaflow) : `GET /ping` renvoie le corps `pong` accompagné de l'en-tête de réponse `METADATA_SERVICE_VERSION: <version>`. Vérifié dans `services/metadata_service/api/admin.py` (`web.Response(text="pong", headers=MultiDict({METADATA_SERVICE_HEADER: METADATA_SERVICE_VERSION}))`, routes enregistrées par `app.router.add_route("GET", "/ping", self.ping)`) et `services/metadata_service/api/utils.py` (`METADATA_SERVICE_HEADER` vaut `"METADATA_SERVICE_VERSION"` et est ajouté à chaque réponse par `format_response` / `web_response`) ; aucun handler ne déclare de jeton ni de dépendance d'authentification. C'est l'en-tête, propre au produit, qu'il faut matcher — jamais le corps `pong` seul, qui est générique. Confirmer avec `GET /flows`, qui énumère tous les flows enregistrés ; noter que la même application expose `GET /auth/token`, qui renvoie des identifiants AWS STS lorsque le service tourne avec un rôle IAM. Sévérité high.
- [x] `templates/exposure/flyteadmin-api-exposed.yaml` — FlyteAdmin (plan de contrôle de Flyte, orchestrateur de workflows ML ; son API gRPC est exposée en REST par un gRPC-gateway) : `GET /api/v1/projects` renvoie `{"projects":[{"id":...,"name":...,"domains":[{"id":...,"name":...}],"description":...,"labels":...,"state":...,"org":...}],"token":...}` et `GET /api/v1/version` renvoie `{"controlPlaneVersion":{"Build":...,"Version":...,"BuildTime":...}}`. Vérifié dans `flyteidl/protos/flyteidl/service/admin.proto` (`rpc ListProjects` porte `option (google.api.http) = { get: "/api/v1/projects" }`, `rpc GetVersion` porte `get: "/api/v1/version"`), `flyteidl/protos/flyteidl/admin/project.proto` (champs exacts des messages `Project` / `Domain` / `Projects`), `flyteidl/protos/flyteidl/admin/version.proto` (champs `Build` / `Version` / `BuildTime`, en capitales — casse inhabituelle en JSON, donc signature discriminante) et côté serveur `flyteadmin/pkg/config/config.go` (`ServerSecurityOptions.UseAuth`, non renseigné dans `defaultServerConfig`, vaut donc `false`) + `flyteadmin/pkg/server/service.go` (les intercepteurs d'authentification, les handlers `auth.RegisterHandlers` et les options de métadonnées du gateway ne sont montés que dans des blocs `if cfg.Security.UseAuth`). Sans `useAuth`, le mux du gateway répond donc sans jeton et sert l'intégralité de l'API d'administration. Matcher sur le tableau `projects` contenant des objets à clé `domains`, puis confirmer sur `controlPlaneVersion`. Sévérité high.
- [x] `templates/exposure/optuna-dashboard-exposed.yaml` — Optuna Dashboard (optuna/optuna-dashboard, tableau de bord web pour les études d'optimisation d'hyperparamètres Optuna) : `GET /api/studies` renvoie `{"study_summaries":[{"study_id":...,"study_name":...,"directions":[...],"user_attrs":{...},"is_preferential":...}]}`. Vérifié dans `optuna_dashboard/_app.py` (route `@app.route("/api/studies")` ; aucun décorateur ni vérification d'authentification sur l'ensemble des routes `/api/...`, y compris celles en écriture — `DELETE /api/studies/<study_id>`, `POST /api/trials/<trial_id>/tell`) et `optuna_dashboard/_serializer.py` (`serialize_frozen_study()` produit exactement ces clés). Le trio `study_summaries`/`is_preferential`/`directions` est propre à Optuna Dashboard, qui n'embarque aucune couche d'authentification — l'accès en écriture non protégé permet en prime de fausser silencieusement des expériences de tuning. Sévérité high.
- [x] `templates/exposure/mlrun-client-spec-exposed.yaml` — MLRun (`mlrun/mlrun`, plateforme MLOps qui orchestre entraînements et fonctions serverless via Nuclio ; l'API `httpdb` écoute par défaut sur le port 8080) : `GET /api/v1/client-spec` renvoie le schéma `ClientSpec` complet — `version`, `nuclio_version`, `ui_url`, `artifact_path`, `namespace`, `docker_registry`, `feature_store_data_prefixes`, `default_function_priority_class_name`, `spark_operator_version`, `igz_version`, `default_runtime_image_by_kind`, `authentication_mode`. Vérifié dans `mlrun/common/schemas/_v1/client_spec.py` (champs exacts de `ClientSpec`), `server/py/services/api/api/endpoints/client_spec.py` (route `GET /client-spec`, dont la seule dépendance est `Depends(get_cached_client_spec)` — aucun `authenticate_request`), `mlrun/db/httpdb.py` (le client construit `f"{base_url}/api/{config.api_base_version}/client-spec"`, d'où `/api/v1/client-spec`) et surtout `mlrun/config.py`, où la section `httpdb` déclare `"authentication": {"mode": "none",  # one of none, basic, bearer, iguazio, iguazio-v4 ...}` : en configuration par défaut l'API d'administration entière répond sans identifiant. Le trio `nuclio_version` + `artifact_path` + `ui_url` est propre à MLRun et toujours renseigné (`version=config.version`, `nuclio_version=...resolve_nuclio_version()`, `ui_url=config.resolve_ui_url()`) — c'est la signature à matcher, jamais le statut HTTP. Nuance à respecter dans le template : `authentication_mode` passe par `_get_config_value_if_not_default('httpdb.authentication.mode')`, il est donc nul ou absent *précisément* quand le mode par défaut `none` est en vigueur — son absence confirme l'exposition, mais ne doit pas servir de matcher positif. Sévérité high.
- [ ] `templates/exposure/tensorboard-exposed.yaml` — TensorBoard (`tensorflow/tensorboard`, le tableau de bord de suivi d'entraînement le plus déployé, servi par `tensorboard --logdir ... --bind_all`) : `GET /data/plugins_listing` renvoie l'inventaire des plugins chargés, chaque entrée portant les clés `enabled`, `loading_mechanism`, `remove_dom`, `disable_reload` et `tab_name`, le `loading_mechanism` étant lui-même un objet typé (`{"type":"NG_COMPONENT"}`, `{"type":"CUSTOM_ELEMENT","element_name":...}`, `{"type":"IFRAME","module_path":...}` ou `{"type":"NONE"}`). Vérifié dans `tensorboard/backend/application.py` (`DATA_PREFIX = "/data"`, `PLUGINS_LISTING_ROUTE = "/plugins_listing"`, et le dict `output_metadata` de `_serve_plugins_listing` avec exactement ces clés) et `tensorboard/plugins/core/core_plugin.py` (routes `"/data/runs"`, `"/data/environment"`, `"/data/logdir"`, `"/data/experiments"`). Aucune authentification n'existe dans le produit : la seule couche montée est `AuthContextMiddleware` (`tensorboard/backend/auth_context_middleware.py`), dont le `__call__` se contente d'injecter un `AuthContext` dans l'environ WSGI puis fait `return self._application(environ, start_response)` — il ne rejette jamais une requête ; l'aide du flag `--bind_all` annonce elle-même « Serve on all public interfaces. This will expose your TensorBoard instance to the network on both IPv4 and IPv6 (where available). » Confirmer avec `GET /data/environment`, qui renvoie `version`, `data_location` (le chemin du logdir côté serveur, souvent un URI S3/GCS), `window_title`, `experiment_name`, `experiment_description` et `creation_time`, puis `GET /data/runs`, qui énumère les noms des runs d'entraînement. Matcher sur le couple `loading_mechanism` + `remove_dom` sur le chemin `/data/plugins_listing`, jamais sur le statut HTTP. Sévérité medium.

## Observabilité LLM

- [x] `templates/exposure/arize-phoenix-exposed.yaml` — Arize Phoenix : `GET /arize_phoenix_version` renvoie la version en texte brut sur un chemin propre au produit, à confirmer par `GET /v1/projects` qui renvoie `{"data":[{"id":...,"name":...,"description":...}],"next_cursor":...}` — l'authentification n'est câblée que si `authentication_enabled` est activé, donc les traces LLM (prompts et réponses) sont lisibles par défaut. Sévérité high.
- [x] `templates/exposure/langfuse-health-exposed.yaml` — Langfuse (observabilité LLM auto-hébergée) : `GET /api/public/health` renvoie `{"status":...,"version":...}`. Vérifié dans `web/src/pages/api/public/health.ts` : le handler n'applique que le middleware CORS, aucun contrôle d'authentification — la version de l'instance est donc lisible par un appelant anonyme. Matcher sur la présence conjointe de `status` et `version` sur ce chemin propre au produit. Sévérité medium.
- [ ] `templates/exposure/opik-projects-exposed.yaml` — Opik (Comet, plateforme d'observabilité LLM open source auto-hébergée via docker-compose, dépôt très actif en 2026 — 21k+ étoiles) : `GET /api/v1/private/projects` renvoie une page de projets avec les champs `id`, `name`, `trace_count`, `thread_count`, `total_estimated_cost`, `guardrails_failed_count`, `error_count`, propres à Opik. Vérifié dans `apps/opik-backend/src/main/java/com/comet/opik/infrastructure/auth/AuthModule.java` : le binding du service d'authentification choisit `AuthServiceImpl` (implémentation no-op qui laisse passer la requête) dès que `authentication.enabled` est faux — valeur par défaut de la configuration OSS en self-host, l'authentification distante (`RemoteAuthService`) n'étant utilisée que si elle est explicitement activée. Le chemin passe par le préfixe nginx `/api/` (rewrite vers `/v1/private/projects` côté back-end) exposé par défaut sur le port 5173. L'accès révèle en prime les traces/prompts et coûts LLM du projet. Sévérité high.

## Traitement de documents pour RAG

- [ ] `templates/exposure/docling-serve-version-exposed.yaml` — Docling Serve (docling-project/docling-serve, API HTTP officielle de Docling — le pipeline de conversion de documents pour RAG maintenu par IBM, dépôt actif en 2026) : `GET /version` renvoie `{"docling-serve":...,"docling-jobkit":...,"docling":...,"docling-core":...,"docling-ibm-models":...,"docling-parse":...,"python":...,"plaform":...}` (la clé `plaform`, coquille conservée dans le code source, est en elle-même un fingerprint fiable). Vérifié dans `docling_serve/helper_functions.py` (dict `DOCLING_VERSIONS`, clés exactes), `docling_serve/app.py` (route `GET /version`, gardée uniquement par le flag `show_version_info` qui vaut `True` par défaut) et `docling_serve/auth.py` (`APIKeyAuth` : la comparaison `header_api_key == self.api_key or self.api_key == ""` laisse passer toute requête tant que `docling_serve_settings.api_key` — qui vaut `""` par défaut dans `docling_serve/settings.py` — n'est pas configuré). Confirme au passage que l'API de conversion (`POST /v1/convert/source`, qui accepte une URL arbitraire à récupérer côté serveur) est elle aussi joignable sans authentification. Port par défaut 5001. Sévérité medium.

## CVE sans template amont

- [x] `templates/cves/CVE-2026-0770.yaml` — Langflow, présent au catalogue CISA KEV, aucun template amont. Vérifier l'avis avant d'écrire le matcher.
- [x] `templates/cves/CVE-2026-55255.yaml` — Langflow, également au KEV.

## Outillage du dépôt

- [x] Ajouter `scripts/coverage.py` qui compare les identifiants du pack à ceux de `nuclei-templates` et signale les doublons, avec un test associé.
- [x] Ajouter au README un tableau de couverture généré par `scripts/coverage.py`.
- [x] Ajouter un test vérifiant que deux templates ne partagent pas le même couple (endpoint, produit).
